import { renderHook } from "@testing-library/react-native";
import { useBackgroundAnimation } from "../useBackgroundAnimation";


describe("tests if useTapListener returns all neccessary handlers", () => {
  it("should return an object with three keys", () => {
    // const { result } = renderHook(() => useBackgroundAnimation());
  });
});